This program is an implementation of the Quickselect algorithm. It uses the median-of-medians pivot with groups of size 7.
Input: An array A of n integers and an integer k in the set {1,2,...,n}.
Output: The k'th smallest element of A.

The implementation is O(n).